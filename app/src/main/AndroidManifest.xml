<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.materialdesigndemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.hzq.recycler.swipe.RecyclerViewSwipeActivity"
            android:label="SwipeLayout"/>

        <activity
            android:name="com.hzq.recycler.swipe.RecyclerViewSwipeBtnActivity"
            android:label="BtnSwipeLayout"/>

        <activity
            android:name="com.hzq.recycler.RecyclerViewActivity"
            android:label="RecycleView" />

        <activity android:name="com.hzq.recycler.RecyclerItemAnimatorActivity"/>
    </application>

</manifest>